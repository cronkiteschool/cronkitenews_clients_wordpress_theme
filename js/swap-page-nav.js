/**
 * swap-page-nav.js
 *
 * Handles setting the ASU searchbox to use local wordpress site for searches if selected.
 */

( function( $ ) {
	$('.post-navigation').insertBefore('.post');
} )( jQuery );
